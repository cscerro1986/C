module main;

import std.stdio;

int main(string[] args)
{
    writefln("Hello World\n");
	return 0;
}
*/
asda
 calculadora.h prototipos  vamos a tener los prototipos o sea llamarlos prototipos o declaraciones.
 calculadora.c vamos a tener las funciones cuerpoes / definiciones.

 #include<stdio.h>
 #include<stdilib.h>
#include<calculadora.h>
#include<calculadora.c>

#include "stdio.h" si las haces entre comillas en lugar de mayor y menor... funciona pero no es lo mismo...
si la biblioteca estandar la pongo entre comillas pero la de calculadora entre mayor y menor... esta no va a funcionar.
porque el programa va a buscar lo que este entre mayor y menor en el directorio.

cuando lo pongo entre comillas va a buscar la biblioteca en primper lugar a la direccion de donde esta el proyecto.

existe la ruta absoluta y relativa.



asdasd
/*
